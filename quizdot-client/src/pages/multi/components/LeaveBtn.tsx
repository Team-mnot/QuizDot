import { Button } from '@/shared/ui';

export function LeaveBtn() {
  const handleClickExit = () => {};

  return (
    <div>
      <Button className="" value="퇴장" onClick={handleClickExit} />
    </div>
  );
}
